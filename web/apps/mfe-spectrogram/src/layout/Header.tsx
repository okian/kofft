/**
 * Legacy re-export for the Header component and its helper functions.
 * This file exists to preserve historical import paths while the actual
 * implementation lives under components/layout.
 */
export { Header, getHeaderClasses } from "../components/layout/Header";
